# SnapFill - MVP 개발 로드맵

---

## 🔧 BLOCK 1: 초기 셋업 + 리스크 검증

### 목표
- 개발 환경 구성 및 의존성 설치
- **핵심 리스크 검증: OCR 정확도 확인** (막판에 발견되면 실패하므로 우선 검증)
- 프로젝트 폴더 구조 생성

### 세부 작업 체크리스트

- [ ] Python 3.9+ 설치 확인 및 가상환경 생성 (`venv`)
- [ ] 필수 패키지 설치
  - [ ] `pip install fastapi uvicorn python-multipart pillow pytesseract opencv-python python-dotenv`
  - [ ] `pip install sqlalchemy` (DB ORM)
  - [ ] `pip install requests` (Claude API 호출)
- [ ] Tesseract OCR 시스템 설치 (Windows: .exe 다운로드 및 설치)
- [ ] `.env` 파일 생성 및 Claude API Key 설정
- [ ] 프로젝트 디렉토리 구조 생성
  ```
  snapfill/
  ├── backend/
  │   ├── app.py (FastAPI 메인 파일)
  │   ├── models.py (DB 모델)
  │   ├── ocr_processor.py (OCR 처리)
  │   ├── ai_classifier.py (AI 분류/추출)
  │   ├── database.py (DB 설정)
  │   └── uploads/ (이미지 저장 폴더)
  ├── frontend/
  │   ├── src/
  │   │   ├── App.jsx
  │   │   ├── components/
  │   │   └── index.css
  │   └── package.json
  ├── .env
  └── README.md
  ```

### 🔴 리스크 검증: OCR 정확도 테스트

**왜 우선인가?** Tesseract OCR 설치 실패 또는 정확도 부족 시 전체 프로젝트가 불가능. 막판에 발견되면 일정 초과.

**검증 방법:**
- [ ] 테스트용 보험 신청서 이미지 생성 (목업 데이터: 이름, 나이, 질병 이력)
- [ ] 테스트용 영수증 이미지 생성 (목업 데이터: 상호명, 금액, 날짜)
- [ ] Tesseract를 이용해 각 이미지에서 텍스트 추출 테스트
- [ ] **성공 기준:** 각 이미지에서 핵심 텍스트(이름, 금액, 날짜 등) 80% 이상 추출

**검증 실패 시 대응:**
- Tesseract 대신 EasyOCR 또는 OpenCV 기반 OCR로 전환
- 이미지 전처리 (회색조 변환, 노이즈 제거) 추가

### 완료 기준 (Definition of Done)

- ✅ 가상환경 활성화 상태
- ✅ 모든 필수 패키지 import 성공
- ✅ Tesseract OCR 실행 가능 (커맨드라인에서 `tesseract --version` 실행 성공)
- ✅ OCR 정확도 테스트 통과 (테스트 이미지 텍스트 추출 80% 이상)
- ✅ 폴더 구조 생성 완료
- ✅ `.env` 파일에 Claude API Key 설정

---

## 🤖 BLOCK 2: AI 분류 및 필드 추출 프롬프트 검증

### 목표
- Claude API 연결 확인
- **핵심 리스크 검증: AI 분류/필드 추출 프롬프트 정확도** (나머지 로직의 기반)
- 최적 프롬프트 찾기 및 확정

### 세부 작업 체크리스트

- [ ] Claude API 호출 기본 테스트 (`requests` 라이브러리 또는 `anthropic` SDK)
- [ ] 분류 프롬프트 작성 및 테스트
  ```
  프롬프트 예시:
  "다음은 OCR로 추출한 텍스트입니다. 이것이 '보험 신청서' 또는 '영수증' 
  중 어느 것인지 판단하고, JSON 형식으로 
  {"document_type": "insurance" 또는 "receipt", "confidence": 0.95}로 응답하세요."
  ```
  - [ ] 테스트 OCR 텍스트 (보험 신청서) → 분류 결과 확인
  - [ ] 테스트 OCR 텍스트 (영수증) → 분류 결과 확인
  - [ ] **성공 기준:** 2개 모두 정확히 분류 (confidence 0.85 이상)

- [ ] 보험 신청서 필드 추출 프롬프트 작성 및 테스트
  ```
  프롬프트 예시:
  "다음 텍스트에서 보험 신청서의 필드를 추출하세요. 
  필드: 이름(applicant_name), 나이(age), 질병 이력(medical_history)
  JSON 형식으로 응답하되, 없는 필드는 null을 입력하세요."
  ```
  - [ ] 테스트 데이터로 프롬프트 실행
  - [ ] **성공 기준:** 3개 필드 모두 추출 (또는 명확한 null 처리)

- [ ] 영수증 필드 추출 프롬프트 작성 및 테스트
  ```
  프롬프트 예시:
  "다음 텍스트에서 영수증의 필드를 추출하세요.
  필드: 상호명(merchant_name), 금액(total_amount), 날짜(transaction_date)
  JSON 형식으로 응답하되, 없는 필드는 null을 입력하세요."
  ```
  - [ ] 테스트 데이터로 프롬프트 실행
  - [ ] **성공 기준:** 3개 필드 모두 추출 (또는 명확한 null 처리)

- [ ] 프롬프트 최적화 (필요시)
  - [ ] 응답 형식이 일관적인지 확인
  - [ ] 마크다운 형식 제거 (JSON만 순수하게)
  - [ ] 한국어 필드명 명확화

- [ ] `ai_classifier.py` 파일 작성
  - [ ] `classify_document(ocr_text)` 함수 구현
  - [ ] `extract_insurance_fields(ocr_text)` 함수 구현
  - [ ] `extract_receipt_fields(ocr_text)` 함수 구현

### 🔴 리스크 검증: AI 프롬프트 정확도 테스트

**왜 우선인가?** 프롬프트가 부정확하면 Backend/Frontend 개발이 완료되어도 결과가 쓸모없음.

**검증 실패 시 대응:**
- 프롬프트 단순화 (너무 복잡한 조건 제거)
- JSON 파싱 오류 처리 추가 (try-except)
- 필드 추출 실패 시 기본값 설정

### 완료 기준 (Definition of Done)

- ✅ Claude API 연결 성공 (API 응답 수신 확인)
- ✅ 분류 프롬프트 테스트 통과 (보험/영수증 정확히 분류)
- ✅ 보험 신청서 필드 추출 테스트 통과 (3개 필드 추출)
- ✅ 영수증 필드 추출 테스트 통과 (3개 필드 추출)
- ✅ `ai_classifier.py` 작성 및 함수 임포트 확인
- ✅ 예상 API 비용 계산 완료 및 임계값 설정 (필요시 rate limiting 추가)

---

## 🔌 BLOCK 3: Backend API 개발

### 목표
- FastAPI 서버 생성
- 이미지 업로드 API 구현
- OCR + AI 파이프라인 통합
- DB 저장 기능 구현
- API 에러 처리

### 세부 작업 체크리스트

#### 4.1 Database 초기화
- [ ] `database.py` 작성
  - [ ] SQLite 설정 (또는 MySQL)
  - [ ] SQLAlchemy ORM 설정
  - [ ] tables 생성 함수 (`create_tables()`)

- [ ] `models.py` 작성
  - [ ] `Document` 모델 정의 (documents 테이블)
  - [ ] `InsuranceRecord` 모델 정의
  - [ ] `ReceiptRecord` 모델 정의
  - [ ] Pydantic 스키마 정의 (API 응답용)

- [ ] 데이터베이스 초기화 (테이블 생성)

#### 4.2 FastAPI 앱 구축
- [ ] `app.py` 메인 파일 작성
  - [ ] FastAPI 앱 인스턴스 생성
  - [ ] CORS 설정 (localhost 허용)
  - [ ] `/health` 헬스체크 엔드포인트 추가

#### 4.3 업로드 & 처리 API
- [ ] `POST /upload` 엔드포인트 구현
  - [ ] 파일 검증 (MIME 타입: jpg, png만)
  - [ ] 파일 크기 검증 (5MB 이하)
  - [ ] 파일 임시 저장
  - [ ] OCR 처리 호출
  - [ ] AI 분류 호출
  - [ ] 문서 타입별 필드 추출 호출
  - [ ] DB에 저장
  - [ ] 임시 파일 삭제
  - [ ] JSON 응답 반환

- [ ] 에러 처리
  - [ ] 파일 형식 오류 → HTTP 400
  - [ ] 파일 크기 초과 → HTTP 413
  - [ ] OCR 실패 → 사용자 친화적 에러 메시지
  - [ ] 분류 실패 → 사용자 친화적 에러 메시지
  - [ ] DB 저장 실패 → HTTP 500

#### 3.4 조회 API (선택사항)
- [ ] `GET /documents` - 처리 완료된 문서 목록 조회
- [ ] `GET /documents/{id}` - 특정 문서 상세 조회

### 완료 기준 (Definition of Done)

- ✅ FastAPI 서버 실행 가능 (`uvicorn app:app --reload` 실행 성공)
- ✅ `/health` 엔드포인트 응답 확인
- ✅ `POST /upload` 정상 작동
  - ✅ 보험 신청서 이미지 업로드 → 올바른 필드 추출 및 저장
  - ✅ 영수증 이미지 업로드 → 올바른 필드 추출 및 저장
- ✅ 에러 처리 검증
  - ✅ 잘못된 파일 형식 업로드 → 에러 메시지
  - ✅ 5MB 이상 파일 업로드 → 에러 메시지
  - ✅ 빈 파일 업로드 → 에러 메시지
- ✅ DB에 데이터 정상 저장 확인 (SELECT 쿼리 실행)
- ✅ 모든 API 응답이 JSON 형식이고 일관성 있음

---

## 🎨 BLOCK 4: Frontend UI 개발

### 목표
- React 또는 Vue 앱 생성
- 이미지 업로드 UI 구현
- 로딩 상태 표시
- 결과 표시 (보험/영수증 분리)
- 에러 메시지 표시

### 세부 작업 체크리스트

#### 4.1 프로젝트 초기화
- [ ] Node.js / npm 설치 확인
- [ ] React 프로젝트 생성 (`create-react-app` 또는 `vite`)
- [ ] Tailwind CSS 설치 및 설정
- [ ] axios 또는 fetch 설정

#### 4.2 UI 컴포넌트 개발
- [ ] `App.jsx` 메인 컴포넌트
  - [ ] 상태 관리 (`useState`: file, loading, result, error)
  - [ ] 파일 업로드 입력 필드
  - [ ] 제출 버튼
  - [ ] 로딩 스핀너 (업로드 중일 때)
  - [ ] 결과 표시 영역 (조건부 렌더링)

- [ ] `UploadForm.jsx` 컴포넌트
  - [ ] 파일 입력 필드 (`<input type="file">`)
  - [ ] 파일 크기/형식 사전 검증 (프론트)
  - [ ] 제출 버튼
  - [ ] 선택된 파일명 표시

- [ ] `ResultsView.jsx` 컴포넌트
  - [ ] 문서 타입 표시 (보험/영수증)
  - [ ] 보험 신청서 결과 (이름, 나이, 질병 이력)
  - [ ] 영수증 결과 (상호명, 금액, 날짜)
  - [ ] "다시 시도" 버튼

- [ ] `ErrorAlert.jsx` 컴포넌트
  - [ ] 에러 메시지 표시
  - [ ] 닫기 버튼

#### 4.3 API 호출 로직
- [ ] Backend와의 통신
  - [ ] `POST /upload` 호출 함수
  - [ ] 요청 헤더 설정 (multipart/form-data)
  - [ ] 응답 파싱 및 상태 업데이트
  - [ ] 에러 처리

#### 4.4 스타일링
- [ ] Tailwind CSS로 레이아웃 구성
  - [ ] 헤더 (앱 제목)
  - [ ] 업로드 영역 (드래그 가능하면 추가 점수)
  - [ ] 결과 영역 (카드 형식)
  - [ ] 반응형 디자인 (모바일 친화적)

### 완료 기준 (Definition of Done)

- ✅ React 앱 실행 가능 (`npm start`)
- ✅ 파일 선택 및 업로드 가능
- ✅ 로딩 중 스핀너 표시
- ✅ 성공 응답 → 보험/영수증 결과 정확히 표시
- ✅ 실패 응답 → 에러 메시지 표시
- ✅ UI가 직관적이고 사용하기 쉬움
- ✅ 브라우저 콘솔 에러 없음

---

## 🔗 BLOCK 5: End-to-End 통합 + 버그 픽스

### 목표
- Backend와 Frontend 완전 통합
- 전체 파이프라인 동작 확인 (이미지 업로드 → 분류 → 추출 → 저장 → 표시)
- 버그 발견 및 수정
- 성능 최적화
- 에러 엣지 케이스 테스트

### 세부 작업 체크리스트

#### 5.1 통합 테스트
- [ ] **시나리오 1: 보험 신청서 정상 처리**
  - [ ] Frontend에서 보험 신청서 이미지 업로드
  - [ ] Backend OCR 처리 확인
  - [ ] AI 분류 "insurance" 확인
  - [ ] 필드 추출 (이름, 나이, 질병 이력) 확인
  - [ ] DB 저장 확인
  - [ ] Frontend 결과 표시 확인

- [ ] **시나리오 2: 영수증 정상 처리**
  - [ ] Frontend에서 영수증 이미지 업로드
  - [ ] Backend OCR 처리 확인
  - [ ] AI 분류 "receipt" 확인
  - [ ] 필드 추출 (상호명, 금액, 날짜) 확인
  - [ ] DB 저장 확인
  - [ ] Frontend 결과 표시 확인

- [ ] **시나리오 3: 에러 처리**
  - [ ] 잘못된 파일 형식 (PDF, DOC 등) 업로드
  - [ ] 파일 크기 초과 (5MB 이상) 업로드
  - [ ] 빈 파일 업로드
  - [ ] 텍스트 없는 이미지 업로드
  - [ ] 각각에 대해 명확한 에러 메시지 표시 확인

#### 5.2 성능 측정
- [ ] 응답 시간 측정
  - [ ] 작은 이미지 (< 1MB)
  - [ ] 중간 이미지 (1~3MB)
  - [ ] 큰 이미지 (3~5MB)
  - [ ] **기준:** 모두 10초 이내 완료

- [ ] 동시 업로드 테스트 (선택사항)
  - [ ] 2개 이미지 동시 업로드 (응답 시간 지표 확인)

#### 5.3 버그 추적 및 수정
- [ ] API 응답 형식 통일성 확인
- [ ] 데이터 타입 검증 (나이는 정수, 금액은 숫자)
- [ ] 한글 텍스트 인코딩 문제 (UTF-8)
- [ ] 이미지 파일명에 특수문자 있을 때 처리
- [ ] CORS 에러 해결 (필요시)
- [ ] 임시 파일 정리 확인

#### 5.4 데이터 무결성 검증
- [ ] DB 쿼리로 저장된 데이터 확인
  ```sql
  SELECT * FROM documents;
  SELECT * FROM insurance_records;
  SELECT * FROM receipt_records;
  ```
- [ ] 추출된 필드가 정확한지 수동 검증
- [ ] NULL 값 처리 확인

#### 5.5 사용자 경험 개선
- [ ] UI 버튼 상태 (업로드 중 비활성화)
- [ ] 로딩 메시지 명확성
- [ ] 결과 페이지 레이아웃 개선 (읽기 쉽게)
- [ ] 다시 시도 버튼 작동 확인

### 버그 수정 우선순위

| 우선순위 | 버그 유형 | 예상 시간 |
|---------|---------|---------|
| **P0 (긴급)** | API 연결 실패, 분류 오류, DB 저장 실패 | 15분 |
| **P1 (높음)** | 에러 메시지 미표시, 응답 형식 오류 | 10분 |
| **P2 (중간)** | 스타일 미적 문제, 성능 개선 | 5분 |
| **P3 (낮음)** | 부가 기능 미구현 | 스킵 |

### 완료 기준 (Definition of Done)

- ✅ **E2E 파이프라인 완전 작동**
  - ✅ 이미지 선택 → 업로드 → 처리 → 결과 표시 완료
  - ✅ 보험/영수증 모두 정상 분류 및 추출
  
- ✅ **모든 에러 케이스 처리**
  - ✅ 파일 형식 오류 → 에러 메시지
  - ✅ 파일 크기 오류 → 에러 메시지
  - ✅ OCR 실패 → 에러 메시지
  - ✅ 분류 실패 → 에러 메시지
  
- ✅ **성능 기준 충족**
  - ✅ 10초 이내 응답 시간
  
- ✅ **데이터 정확성**
  - ✅ DB에 모든 데이터 정확히 저장
  - ✅ 필드 타입 일치 (나이 = 정수, 금액 = 숫자 등)
  
- ✅ **UI/UX 완성**
  - ✅ 사용자 인터페이스 직관적
  - ✅ 모바일 반응형 확인 (선택사항)
  - ✅ 브라우저 콘솔 에러 없음

---

## 🚀 BLOCK 6: 최종 점검 및 마무리

### 목표
- 최종 점검
- 데모 준비
- 문서화 완성

### 세부 작업 체크리스트

- [ ] 전체 코드 리뷰 (문법, 주석 확인)
- [ ] README.md 작성 (실행 방법, 기능 설명)
  ```markdown
  # SnapFill MVP
  
  ## 실행 방법
  1. Backend: python -m uvicorn app:app --reload
  2. Frontend: npm start
  3. localhost:3000 접속
  
  ## 기능
  - 보험 신청서/영수증 자동 분류
  - 핵심 필드 자동 추출
  ```

- [ ] 데모용 이미지 확인
  - [ ] 보험 신청서 목업 이미지 (고품질)
  - [ ] 영수증 목업 이미지 (고품질)

- [ ] 로그 메시지 정리 (디버그 로그 제거)
- [ ] 커밋 메시지 작성 및 커밋 준비
- [ ] 최종 성공 지표 측정
  - [ ] 분류 정확도
  - [ ] 필드 추출 성공률
  - [ ] 응답 시간

### 완료 기준 (Definition of Done)

- ✅ 코드 최종 리뷰 완료
- ✅ README.md 작성 완료
- ✅ 데모용 이미지 준비 완료
- ✅ 커밋 및 푸시 준비 완료 (또는 로컬 git 초기화)
- ✅ 성공 지표 문서화

---

## 🚨 리스크 관리 전략

### 높은 리스크 (사전 예방 필수)

| 리스크 | 영향 | 대응 방안 |
|--------|------|---------|
| **OCR 정확도 부족** | 전체 시스템 무용지물 | Block 1에서 즉시 검증, 실패 시 대체 솔루션 준비 |
| **AI 프롬프트 오류** | 잘못된 분류/추출 | Block 2에서 프롬프트 최적화, 다양한 샘플 테스트 |
| **API 연결 실패** | MVP 미완성 | 에러 로깅 및 디버깅, 대체 API 고려 |

### 중간 리스크 (모니터링)

| 리스크 | 대응 방안 |
|--------|---------|
| **API 비용 초과** | 토큰 제한 설정, 프롬프트 단순화 |
| **CORS 에러** | localhost 허용, 필요시 프록시 설정 |
| **인코딩 문제** | UTF-8 명시, 한글 테스트 |

### 일정 초과 시 우선순위

```
지속 유지: Backend API (필수)
지속 유지: Frontend 기본 UI (필수)
우선 삭제: 미니 기능 (결과 조회 API, 드래그 & 드롭, 다국어)
우선 삭제: 최적화 (캐싱, 배치 처리)
우선 삭제: 추가 검증 (서명 감지, 품질 점수)
```

---

## 📊 진행도 추적

각 블록 종료 후 아래 체크리스트로 진행도 확인:

### Block 1 종료
- [ ] 환경 구성 완료
- [ ] OCR 정확도 검증 통과
- [ ] 폴더 구조 생성

### Block 2 종료
- [ ] Claude API 연결 완료
- [ ] 분류 프롬프트 검증 통과
- [ ] 필드 추출 프롬프트 검증 통과

### Block 3 종료
- [ ] FastAPI 서버 실행 중
- [ ] `/upload` API 정상 작동
- [ ] DB 저장 확인

### Block 4 종료
- [ ] React 앱 실행 중
- [ ] 업로드 폼 완성
- [ ] 결과 표시 UI 완성

### Block 5 종료
- [ ] E2E 파이프라인 완전 작동
- [ ] 모든 주요 버그 수정
- [ ] 성공 지표 충족

### Block 6 종료
- [ ] 최종 점검 완료
- [ ] 데모 준비 완료
- [ ] 문서화 완성

---

## 📝 주의사항

1. **기능 범위 관리**
   - Out of Scope 기능 절대 추가 금지
   - MVP 완성에 필요한 핵심 기능에 집중

2. **리스크 선 검증**
   - OCR과 AI 프롬프트는 초기에 테스트
   - 막판에 실패하면 전체 프로젝트 실패

3. **커밋 및 백업**
   - 각 블록 종료 후 코드 커밋
   - 최신 코드 항상 원격 저장소에 푸시

4. **코드 품질**
   - 각 블록마다 코드 리뷰 수행
   - 에러 처리 및 로깅 충실히 하기

---

**이 로드맵은 필요에 따라 유동적으로 조정 가능합니다.**

Document Version: 2.0  
Updated: 2026-08-02  
Status: In Progress
